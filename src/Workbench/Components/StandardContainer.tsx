import { Box, styled } from '@mui/material/';

export const StandardContainer = styled(Box)(({}) => ({
  display: 'flex',
  flexDirection: 'column',
  paddingTop: 40,
  margin: '0 auto',
  width: '55.5vw',
  paddingLeft: 5,
  paddingRight: 5,
  paddingBottom: 40
}));
