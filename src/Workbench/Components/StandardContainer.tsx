import { Box, styled } from '@mui/material/';

export const StandardContainer = styled(Box)(({}) => ({
  display: 'flex',
  flexDirection: 'column',
  paddingTop: 40,
  margin: 'auto',
  width: '55.5vw',
  paddingBottom: 40
}));
