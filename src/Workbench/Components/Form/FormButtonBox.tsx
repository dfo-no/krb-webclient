import { Box, styled } from '@mui/material';

export const FormButtonBox = styled(Box)(() => ({
  display: 'flex',
  alignSelf: 'center',
  flexGrow: 1,
  flexDirection: 'row',
  marginLeft: 'auto'
}));
