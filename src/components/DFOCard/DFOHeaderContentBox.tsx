import { Box, styled } from '@mui/material';

export const DFOHeaderContentBox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  padding: 15,
  paddingLeft: 70,
  paddingRight: 70
}));
