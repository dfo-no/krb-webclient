import makeStyles from '@mui/styles/makeStyles';

export const useFormStyles = makeStyles({
  flexGrowForm: {
    display: 'flex',
    flexGrow: 1
  },
  singlePageForm: {
    width: '100%',
    height: '100%'
  }
});
